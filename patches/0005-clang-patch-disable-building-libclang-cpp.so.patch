From 40e62611a06e5c8213e51a0910d93fa77b44d717 Mon Sep 17 00:00:00 2001
From: Andrew Kelley <andrew@ziglang.org>
Date: Mon, 7 Nov 2022 19:41:52 -0700
Subject: [PATCH 05/11] clang patch: disable building libclang-cpp.so

---
 clang/tools/CMakeLists.txt | 6 ------
 1 file changed, 6 deletions(-)

diff --git a/clang/tools/CMakeLists.txt b/clang/tools/CMakeLists.txt
index a16089be..6188d9a3 100644
--- a/clang/tools/CMakeLists.txt
+++ b/clang/tools/CMakeLists.txt
@@ -30,12 +30,6 @@ if(CLANG_INCLUDE_TESTS)
 endif()
 
 add_clang_subdirectory(clang-refactor)
-# For MinGW/Cygwin we only enable shared library if LLVM_LINK_LLVM_DYLIB=ON.
-# Without that option resulting library is too close to 2^16 DLL exports limit.
-if((UNIX AND NOT CYGWIN) OR (MSVC AND LLVM_BUILD_LLVM_DYLIB_VIS) OR
-  ((MINGW OR CYGWIN) AND LLVM_LINK_LLVM_DYLIB))
-  add_clang_subdirectory(clang-shlib)
-endif()
 
 if(CLANG_ENABLE_STATIC_ANALYZER)
   add_clang_subdirectory(clang-check)
-- 
2.43.0

